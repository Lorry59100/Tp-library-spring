spring:
  application:
    name: library
  datasource:
    url: jdbc:postgresql://localhost:5432/library
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: create-drop
    database:
      driverClassName: org.postgresql.Driver